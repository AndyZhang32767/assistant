__pycache__/
.DS_Store
*.pyc
*.pyo

# Runtime data (contains user sessions, history, denied lists)
data/*.json

# Local environment file (contains real secrets — see .env.example)
.env

# Personal schedule file
schedule
